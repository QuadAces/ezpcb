# PCB Editor Notes

- Units are millimeters throughout editor, flattening, Gerber, and Pick & Place output.
- Board origin is (0,0) and shared by both Gerber and centroid CSV export.
- Run `pip install -r scripts/pcb/requirements.txt` before using Gerber export.
